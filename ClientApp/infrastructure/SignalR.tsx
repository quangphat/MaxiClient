//import { HubConnectionBuilder } from '@aspnet/signalr';
//import * as Utils from './Utils'

//let accountId = Utils.GetAccount() != null ? Utils.GetAccount().personId : ''
//export const hubConnection = new HubConnectionBuilder()
//    .withUrl(`http://admin.greencode.vn:52709/notification?personId=${accountId}`)
//    .build();
//export const createHubConnection = () => {
//    if (Utils.isLogin())
//        hubConnection.start();
//}
